{
  "name": "blockchain-starter",
  "version": "0.1.0",
  "private": true,
  "description": "Minimal Hardhat + TypeScript workspace ready for a new project",
  "type": "module",
  "scripts": {
    "compile": "hardhat compile",
    "clean": "hardhat clean",
    "test": "hardhat test",
    "node": "hardhat node"
  },
  "devDependencies": {
    "@nomicfoundation/hardhat-toolbox-viem": "^5.0.1",
    "@types/node": "^22.19.1",
    "hardhat": "^3.0.15",
    "typescript": "~5.8.0"
  }
}
